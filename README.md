# Langchain_PDF_Groq
o summarize a PDF using LangChain and Groq (leveraging fast models like Llama 3), you can use the LangChain framework to load the document, split it into chunks, and send them to the Groq API for summarization
